# 요구사항 검증표

원본 요구사항 50개를 에픽, 세부 작업, 구현 대상, 테스트와 실행 증거에 연결하는 문서다.

원본 파일은 저장소에 커밋하지 않고 별도 보관한다. 이 표의 요구사항 이름과 검증 목표는 원본 문구를 기준으로 작성한다.

## 사용 상태

- `미착수`: 구현과 테스트가 없음
- `설계 완료`: 설계 문서와 작업 번호는 있으나 구현과 테스트가 없음
- `진행`: 일부 구현 또는 테스트가 있음
- `검증 대기`: 구현은 있으나 실행 증거 확인이 필요함
- `완료`: 구현, 테스트, 실행 증거가 모두 확인됨
- `해석 대기`: 원본만으로 범위를 확정하기 어려움

## 시스템 기능 요구사항

| 요구사항 | 이름 | 에픽과 작업 | 구현 대상 | 테스트 | 증거 | 상태 |
|---|---|---|---|---|---|---|
| SFR-001 | 사용자 로그인 | E2-01, E2-10, E2-11 | 이메일 로그인 API·쿠키 세션·로그인 화면 | `backend/tests/test_auth_api.py`, `frontend/src/App.test.tsx`, `frontend/src/pages/LoginPage.test.tsx` | 2026-08-27: ADMIN/USER 로그인, 일반 401, 토큰 미노출, HttpOnly 쿠키 및 `/auth/me` 기반 프론트 로그인 흐름을 백엔드 전체 pytest와 Vitest 15개로 확인 | 완료 |
| SFR-002 | 인증 수단 발급 | E1-07, E2-01, E2-02, E2-11 | JWT 발급·쿠키 전달, 서명·24시간 만료·활성 계정 검증 | `backend/tests/test_auth_api.py`, `backend/tests/test_auth_authorization.py` | 2026-08-27: 누락·변조·만료·잘못된 sub·비활성 기존 세션의 일반 401, `/auth/me`, 멱등 로그아웃과 Secure 설정 분리를 전체 pytest로 확인 | 완료 |
| SFR-003 | 역할 기반 접근 제어 | E2-03, E2-10, E2-11 | 현재 DB 역할 기반 관리자 API·메뉴 통제 | `backend/tests/test_auth_authorization.py`, `frontend/src/App.test.tsx` | 2026-08-27: 관리자 API 전체의 ADMIN 허용, USER 403, 미인증 401, JWT role claim이 아닌 DB 역할 판정과 USER 메뉴 비노출을 확인. E6에서 새 카탈로그 UI 행동이 추가되면 같은 정책을 재적용 | 진행 |
| SFR-004 | 분석 프로젝트 관리 | E1-02, E1-07, E1-08, E2-03 | 관리자 프로젝트 등록·수정·조회 API | `backend/tests/test_project_api.py`, `backend/tests/test_auth_authorization.py` | 2026-08-27: 프로젝트 CRUD 계약, CSRF, ADMIN 생성·수정 허용 및 USER 403을 전체 pytest로 확인 | 완료 |
| SFR-005 | 프로젝트 사용자 할당 | E1-03, E1-07, E2-06, E2-07 | 이메일 기반 접근권한 부여·목록·해제 | `backend/tests/test_project_access_api.py`, `backend/tests/test_project_resource_access.py`, `frontend/src/App.test.tsx` | 2026-08-27: `user_id`/`user_email` 응답, 404·409·422, 204 해제와 즉시 접근 철회를 API·Drawer 테스트로 확인 | 완료 |
| SFR-006 | 프로젝트 조회 범위 | E1-03, E2-04, E2-05, E2-08 | ADMIN 전체·USER 현재 관계 기반 프로젝트/분석/Finding 조회 | `backend/tests/test_project_resource_access.py`, `backend/tests/test_project_access_api.py` | 2026-08-27: 권한 부여 전 404, 부여 후 200, 해제 후 404와 목록 즉시 제외를 전체 pytest로 확인 | 완료 |
| SFR-007 | 분석 대상 소스 관리 | E3-01, E3-02, E3-03, E3-06, E3-07 | 관리자 ZIP 업로드, 서버 작업영역, 자동 언어 감지 | `backend/tests/test_source_upload_api.py`, `backend/tests/test_source_archive.py`, `backend/tests/test_source_workspace.py`, `frontend/src/pages/SourceUploadDrawer.test.tsx` | 2026-08-28: 관리자 소스 등록, ZIP 자원·경로 검증, Java/JavaScript/Python 자동 감지, DB commit 실패 정리, USER 비노출을 백엔드 184개와 프론트 25개 테스트로 확인 | 완료 |
| SFR-008 | 정적 분석 실행 | E1-07, E4-01, E4-03, E4-04, E4-07, E2-03 | 관리자 분석 실행 API, 단일 실행기 | `backend/tests/test_analysis_api.py`, `backend/tests/test_analysis_execution.py` | E1-07 완료(2026-08-27): 분석 생성(`POST /api/analyses`)은 관리자 전용으로 `require_admin`+`require_csrf` 사용, 응답은 `AnalysisAdminOut`으로 고정. E4-01/03/04/07 완료(2026-08-28): `ThreadPoolExecutor(max_workers=1)` 단일 실행기가 `PENDING→RUNNING→COMPLETED/FAILED`를 전환하며 Semgrep subprocess를 실행함을 `test_admin_creates_pending_analysis_and_captures_source`, `test_executor_copies_captured_source_and_completes`, `test_executor_records_safe_engine_failure`로 확인. 재현 검증(2026-08-28): 별도 Postgres·venv에서 backend 195개 pytest 전체 통과, ruff 통과 | 완료 |
| SFR-009 | 분석 처리 체계 | E4-01, E4-03, E4-07, E5-01, E5-02, E5-04 | 수집, 분석, 진단, 정규화 흐름 | 분석 처리 테스트 | E4-01/03/07 진행(2026-08-28): 소스 스냅샷 복사 → Semgrep 실행 → JSON 결과 저장까지의 수집·분석 흐름을 `analysis_executor.py`, `semgrep_runner.py`로 구현. 결과를 KISA와 연결하는 정규화(E5-01/02/04)는 설계만 확정(ADR-029~036)되고 구현 전 | 진행 |
| SFR-010 | 분석 언어 확장성 | E4-01, E4-02, E5-02 | 공통 실행기와 결과 모델 | `backend/tests/test_semgrep_runner.py` | E4-01/02 완료(2026-08-28): Semgrep 엔진 하나로 Java/JavaScript/Python을 지원하는 공통 실행기를 구현. 다른 엔진 추가를 위한 파서 확장 구조(E5-02 `NormalizedFinding`)는 설계만 확정 | 진행 |
| SFR-011 | 초기 분석 대상 지원 | E3-06, E4-02, E5-09, E7-06 | Java, JavaScript, Python 분석 | `backend/tests/test_source_archive.py`, `backend/tests/test_semgrep_runner.py` | 2026-08-28: 업로드 단계 자동 감지에 이어, E4-02 완료(2026-08-28)로 실제 Semgrep 분석 실행에서도 Java/JavaScript/Python 정규 파일만 분석 입력으로 필터링함을 `test_collect_analysis_targets_only_includes_supported_regular_source_files`로 확인. 실제 탐지 규칙은 자체 작성 3개(E5-03)뿐이라 언어별 탐지 폭 확장은 E5/E7 잔여 | 진행 |
| SFR-012 | 진단 항목 등록 | E1-06, E5-03, E6-11 | 공통 진단 항목 등록 구조 | 항목 등록 테스트 | E1-06 완료(2026-08-27): `POST /api/catalog`(관리자 전용, `kisa_code` 중복 시 409) 구현, `test_catalog_schema.py`로 정상 등록·중복 거부·권한 거부 검증. 화면(E6-11)은 잔여 | 진행 |
| SFR-013 | 개발보안 가이드 진단 기준 카탈로그 | E1-06, E6-06 | KISA 49개 카탈로그 | 카탈로그 테스트 | E1-06 완료(2026-08-27): 소프트웨어 개발보안 가이드(행안부·KISA, 2021.12.29) 부록 기준 49개 항목을 `backend/app/services/kisa_catalog_seed.py`에 시드로 등록(`docker-compose`의 `python seed.py`에서 실행), 실제 alembic upgrade head + seed.py 흐름으로 49개 삽입과 재실행 시 0개 중복 삽입(멱등성) 확인. 카탈로그 목록 화면은 E6-06 잔여 | 진행 |
| SFR-014 | 분석 결과 표준화 | E1-05, E1-07, E5-02 | 공통 결과 필드와 정규화 | 결과 표준화 테스트 | E1-05 완료(2026-08-27): Finding에 `criterion_id`, `evidence`, `recommendation`, `raw_result` 추가로 완료 조건의 공통 필드(항목 식별자·명칭, 기준 식별자, 언어, 심각도, 신뢰도, 위치, 메시지, 근거, 권고, 원본 결과)를 모두 갖춤. E1-07 완료: `raw_result`를 `FindingAdminOut`에만 남기고 `FindingUserOut`에서 제외. E5 설계 3차 수정(2026-08-28): 공식 Semgrep 규칙 라이선스가 재배포·서비스 제공을 금지함을 확인해 벤더링 계획을 철회하고, SecScan 자체 작성 규칙(KISA-002/005/043 매핑) 기준으로 `engine_rule_id`·심각도·신뢰도·근거·코드 조각 정규화 설계를 재확정(ADR-023, ADR-029~036). 실제 정규화 구현은 E5 잔여 | 진행 |
| SFR-015 | 분석 실행 상태 관리 | E1-04, E4-01, E4-04, E4-07, E4-08, E6-02 | 대기, 진행, 완료, 실패 상태 | `backend/tests/test_analysis_execution.py`, `backend/tests/test_analysis_startup_recovery.py`, `frontend/src/App.test.tsx` | E1-04 완료(2026-08-27): `ck_analyses_status` 체크 제약으로 PENDING/RUNNING/COMPLETED/FAILED만 허용, `test_analysis_schema.py`로 허용·거부 값 검증. E4-01/04/08 완료(2026-08-28): 실제 상태 전환이 구현되고, 서버 재시작 시 남은 PENDING/RUNNING을 FAILED로 전환하는 복구를 `test_startup_marks_pending_and_running_analyses_as_interrupted`로 확인. 프론트 `AnalysisStatusPage.tsx`가 3초 간격 폴링으로 상태를 표시함을 Vitest(App.test.tsx 19개 포함 총 30개, 2026-08-28 직접 재현 통과)로 확인 | 완료 |
| SFR-016 | 분석 결과 조회 | E1-07, E5-07, E5-08, E6-01, E6-04 | 이력, 목록, 상세 조회 | 결과 조회 테스트 | E1-07 완료(2026-08-27): `GET /api/analyses`, `GET /api/analyses/{id}`, `GET /api/findings`, `GET /api/findings/{id}`가 역할별 응답 모델(Admin/User)을 반환하도록 확정, `test_api_contract.py` 9개로 필드 노출 차이 검증. 이력·화면은 E5-07/E5-08/E6-01/E6-04 잔여 | 진행 |
| SFR-017 | 진단 결과 검색 및 필터 | E1-07, E6-03 | 심각도 등 주요 속성 필터 | 필터 테스트 | E1-07 진행(2026-08-27): `GET /api/findings?severity=`는 기존 라우터에 이미 있었고, 이번에 역할별 응답 스키마 적용을 함께 확인(`test_user_finding_list_excludes_raw_result` 등). KISA 매핑 여부 필터·화면은 E6-03 잔여 | 진행 |

## 데이터 요구사항

| 요구사항 | 이름 | 에픽과 작업 | 구현 대상 | 테스트 | 증거 | 상태 |
|---|---|---|---|---|---|---|
| DAR-001 | 데이터 저장소 | E1-08, E1-09 | PostgreSQL과 Alembic | 마이그레이션 테스트 | E1-08 완료(2026-08-27): `test_e1_08_audit_head_schema_matches_erd_exactly`로 head 스키마 6개 테이블(users/projects/project_accesses/analyses/findings/kisa_catalog) 전체 컬럼이 `docs/erd.md`와 완전 일치(초과·누락 없음)함을 확인. `test_e1_08_audit_all_foreign_keys_exist`로 ERD가 정의한 외래키 8개 전부, `test_e1_08_audit_all_uniqueness_and_check_constraints_exist`로 유니크·체크 제약 7개 전부 존재 확인. 감사 중 스키마-ERD 불일치는 발견되지 않음(수정 불필요). 기존 0001~0006 마이그레이션의 개별 upgrade/downgrade/재적용 테스트 20개와 함께 실제 Postgres로 검증. E1-09 완료(2026-08-27): `test_e1_contract.py`(22개)로 6개 엔티티 필수 필드 누락 스윕(모델 레벨 IntegrityError·API 레벨 422)과 로그인→프로젝트 생성→접근권한 부여→분석 생성→분석/카탈로그 조회 엔드투엔드 흐름의 응답 필드가 `docs/erd.md`/`docs/api-contract.md`와 일치함을 확인. 전체 스위트(9개 파일, 115개) 회귀 없이 통과, ruff 통과. E1-08+E1-09로 이 요구사항의 E1 범위 완료 | 완료 |
| DAR-002 | 사용자 데이터 | E1-01, E1-09 | 식별자, 해시, 역할, 활성 상태, 시각 | 사용자 데이터 테스트 | E1-01 완료(2026-08-27): `test_user_model.py`, `test_auth_api.py`, `test_migrations.py` 19개 전부 통과, ruff 통과, Postgres 실제 upgrade/downgrade 검증. E1-09 완료(2026-08-27): `test_e1_contract.py`로 `email`/`password_hash` 누락 시 IntegrityError, 로그인 요청 스키마 누락 필드 422를 재확인. E1-01+E1-09로 이 요구사항의 E1 범위 완료 | 완료 |
| DAR-003 | 프로젝트 데이터 | E1-02, E1-08, E1-09 | 프로젝트 기본 필드와 시각 | 프로젝트 데이터 테스트 | E1-02 완료(2026-08-27): `test_project_api.py`, `test_migrations.py`(0002→0003 업그레이드·다운그레이드) 전부 통과, ruff 통과, Postgres 실제 upgrade/downgrade 검증. ERD(`docs/erd.md`)와 실제 스키마(`0003_project_fields.py`) 일치. E1-08 완료(2026-08-27): head 스키마 감사로 `projects` 컬럼 전체와 `projects.created_by → users.id` FK, `uq_projects_name` 제약을 재확인. E1-09 완료(2026-08-27): `test_e1_contract.py`로 `created_by` 누락 시 IntegrityError(API로는 재현 불가, 모델 레벨로 확인)와 엔드투엔드 흐름에서 `POST /projects` 응답 필드가 `ProjectOut`·ERD와 일치함을 재확인. E1-02+E1-08+E1-09로 이 요구사항의 E1 범위 완료 | 완료 |
| DAR-004 | 프로젝트 권한 데이터 | E1-03, E1-09, E2-06, E2-07 | 사용자와 프로젝트 접근 관계 | 관계 무결성 테스트 | E1-03 완료(2026-08-27): `test_project_access_model.py` 5개 전부 통과(생성, project/user/grantor 관계 로드, 중복 등록 IntegrityError, 사용자·프로젝트 다대다 조합 확인), ruff 통과. `project_accesses`는 마이그레이션 0001에 이미 존재해 신규 리비전 불필요. E1-08 완료(2026-08-27): head 스키마 감사에서 `project_accesses` 컬럼 전체(그동안 `test_migrations.py`에 컬럼 검증이 없던 테이블, 이번에 처음 추가)와 FK 3개(`project_id`/`user_id`/`granted_by`), `uq_project_user` 제약을 재확인. E1-09 완료(2026-08-27): `test_e1_contract.py`로 `project_id`/`user_id`/`granted_by` 각각 누락 시 IntegrityError, `POST /projects/{id}/access`의 `user_id` 누락 422를 재확인. 부여·해제 API의 상세 정책 적용은 E2-06/E2-07 잔여 | 진행 |
| DAR-005 | 분석 실행 데이터 | E1-04, E1-09, E4-01, E4-04, E4-06 | 프로젝트, 엔진, 언어, 상태, 시각, 오류, 요약 | `backend/tests/test_semgrep_runner.py`, `backend/tests/test_analysis_execution.py` | E1-04 완료(2026-08-27): `engine`, `analyzed_languages`, `source_snapshot_location`, `error_code`, `summary` 필드 추가(0004 마이그레이션). 프로젝트별 PENDING/RUNNING 동시 1건 제한은 `uq_analyses_project_active` partial unique index로 DB 레벨 강제. E1-08/E1-09 완료(2026-08-27): head 스키마·`test_e1_contract.py`로 재확인. E4-01/04/06 완료(2026-08-28): 0007 마이그레이션으로 `execution_log`(관리자 전용, 최대 64 KiB)와 스냅샷 위치 필드를 추가, 타임아웃·자원 제한 실패가 `error_code`/`error_message`/`execution_log`로 기록됨을 `test_resource_limit_exit_mapping`, `test_runner_timeout_terminates_the_process_group`으로 확인. 실행 파이프라인 전체(E1+E4 범위) 완료 | 완료 |
| DAR-006 | 진단 결과 데이터 | E1-05, E1-09, E5-01, E5-04, E5-05, E5-06 | 진단 항목, 기준, 위치, 근거, 권고, 원본 결과 | 결과 저장 테스트 | E1-05 완료(2026-08-27): `test_finding_schema.py` 4개 전부 통과(정상 저장, 미매핑 결과의 `kisa_code`/`criterion_id` NULL과 나머지 필드 정상 저장, 스냅샷 불변성, 스키마 필드). 0005 마이그레이션으로 `criterion_id`/`evidence`/`recommendation`/`raw_result` 추가. E1-08/E1-09 완료(2026-08-27): head 스키마·`test_e1_contract.py`로 재확인. E5 설계(2026-08-28): Semgrep 엔진 규칙 식별자를 별도 `engine_rule_id`로 `NOT NULL` 저장해 진단 항목 식별자와 명칭을 분리하기로 결정. E5 설계 3차 수정(2026-08-28): 공식 Semgrep 규칙 라이선스 제약(재배포·서비스 제공 금지) 확인 후 자체 작성 규칙 기준으로 전환(ADR-023 개정)했으며, `backend/semgrep-rules/secscan-security.yml`·`RULES_PROVENANCE.md`는 E4-02에서 이미 구현·병합됨. KISA-002/005/043 3개 매핑, taint 규칙 보완, 언어별 fixture 설계는 확정(ADR-035/036)했고 구현은 E5-01/03/04/05/06/09 잔여 | 진행 |
| DAR-007 | 진단 기준 데이터 | E1-06, E1-09, E6-06, E6-07 | 식별자, 분류, 항목 번호, 참조 정보, 활성 여부, 심각도 | 카탈로그 데이터 테스트 | E1-06 완료(2026-08-27): `KisaCatalog`에 `item_number`, `reference_info`, `active`, 3단계 `implementation_status`(지원/부분 지원/미지원, `ck_kisa_catalog_implementation_status` 체크 제약) 추가. 0006 마이그레이션으로 실제 Postgres에 적용, `test_catalog_schema.py`로 제약·시드 개수 검증. E1-08 완료(2026-08-27): head 스키마 감사로 `kisa_catalog` 컬럼 전체(ERD와 완전 일치)와 `ck_kisa_catalog_implementation_status` 제약을 재확인. E1-09 완료(2026-08-27): `test_e1_contract.py`로 `category`/`name`/`default_severity` 각각 누락 시 IntegrityError(모델), `POST /api/catalog`의 동일 필드 누락 422(API), 엔드투엔드 흐름에서 카탈로그 응답 49개 전부 `CatalogItemOut` 필드와 일치함을 재확인. E6-06/E6-07 잔여 | 진행 |
| DAR-008 | 분석 시점 이력 보존 | E1-04, E1-05, E1-09, E4-01, E5-06 | 분석 시점 진단 정보 스냅샷 | `backend/tests/test_analysis_execution.py` | E1-04, E1-05 완료(2026-08-27): `Analysis.analyzed_languages`(E1-04)에 이어 `Finding.criterion_id`/`recommendation`(E1-05)도 정규화 시점 스냅샷으로 저장. `test_finding_snapshot_is_independent_of_catalog_changes`로 KisaCatalog 값 변경 후에도 Finding 값이 그대로임을 검증. E4-01 완료(2026-08-28): `Analysis.source_snapshot_location`이 실제 분석별 스냅샷 디렉터리(`analyses/{id}/source`)를 가리키고, `RUNNING` 단계에서 현재 프로젝트 소스 전체가 그 위치로 복사됨을 `test_executor_copies_captured_source_and_completes`로 확인. Finding 쪽 원본 결과 스냅샷(E5-06)은 잔여 | 진행 |
| DAR-009 | 구조화된 부가정보 | E1-04, E1-05, E1-09, E5-06 | 분석 요약과 원본 결과 JSON | JSON 보존 테스트 | E1-04 완료(2026-08-27): `Analysis.summary`(JSONB) 필드 추가, 구조화된 dict 저장·조회를 `test_analysis_can_be_created_with_all_new_fields`로 검증. `raw_result`는 기존 필드 유지. Finding 쪽 구조화 데이터는 E1-05 잔여 | 진행 |
| DAR-010 | 데이터 관계 무결성 | E1-03, E1-08, E1-09 | 사용자, 프로젝트, 분석, 결과 관계 | FK와 삭제 정책 테스트 | E1-03 완료(2026-08-27): `project_accesses`의 FK(project_id, user_id, granted_by)와 (project_id, user_id) UNIQUE 제약을 실제 Postgres에서 중복 등록 거부로 검증. 접근권한 해제 시 프로젝트·분석 데이터 미삭제 정책은 삭제 API 자체가 MVP에 없어 데이터 보존이 구조적으로 보장됨(ADR 문서 확인). E1-08 완료(2026-08-27): 6개 테이블 전체에 걸쳐 ERD가 정의한 외래키 8개(`projects.created_by`, `project_accesses.project_id/user_id/granted_by`, `analyses.project_id/executed_by`, `findings.analysis_id/kisa_code`)가 head 스키마에 전부 존재함을 종합 감사로 확인. E1-09 완료(2026-08-27): `test_e1_contract.py`의 필수 필드 스윕 12개(모델 레벨)로 FK 컬럼이 실제로 NOT NULL을 강제함을 관계별로 재확인. E1-03+E1-08+E1-09로 이 요구사항의 E1 범위 완료 | 완료 |

## 보안 요구사항

| 요구사항 | 이름 | 에픽과 작업 | 구현 대상 | 테스트 | 증거 | 상태 |
|---|---|---|---|---|---|---|
| SEC-001 | 비밀번호 보호 | E2-01, E7-01 | bcrypt 해시 검증과 인증 응답 비밀값 제외 | `backend/tests/test_auth_api.py` | 2026-08-27: 없는 사용자·비활성 사용자도 password verification을 수행하고, 로그인 응답이 email·role만 포함함을 전체 pytest로 확인 | 완료 |
| SEC-002 | 보호 기능 인증 | E2-02, E7-01 | HttpOnly 쿠키 세션, 서명·만료·활성 계정 검증 | `backend/tests/test_auth_api.py`, `backend/tests/test_auth_authorization.py` | 2026-08-27: 미인증 관리자 API 401, 누락·변조·만료·잘못된 sub·비활성 세션 401을 전체 pytest로 확인 | 완료 |
| SEC-003 | 관리자 기능 통제 | E1-07, E2-03, E2-09, E6-05, E6-11 | 관리자 API와 내부 오류·원본 결과 제한 | `backend/tests/test_auth_authorization.py`, `backend/tests/test_api_contract.py`, `backend/tests/test_project_resource_access.py` | 2026-08-27: 현재 관리자 API의 USER 403 및 ADMIN/USER 분석·Finding 필드 경계를 확인. E6에서 추가될 카탈로그 화면 행동은 별도 검증 필요 | 진행 |
| SEC-004 | 일반 사용자 권한 통제 | E1-07, E2-03, E2-05, E2-10, E2-11 | USER 읽기 전용, 관계 기반 조회 및 프론트 관리자 행동 비노출 | `backend/tests/test_auth_authorization.py`, `backend/tests/test_project_resource_access.py`, `frontend/src/App.test.tsx` | 2026-08-27: USER 관리자 API 403, 허용 읽기 경로 200, 관리자 행동·Drawer 비노출을 전체 pytest와 Vitest로 확인 | 완료 |
| SEC-005 | 프로젝트 소속 검증 | E2-05, E2-08, E7-02 | 저장된 ProjectAccess 관계 기반 프로젝트·분석·Finding 검증 | `backend/tests/test_project_resource_access.py` | 2026-08-27: 직접 ID 요청을 포함해 현재 관계가 없는 프로젝트·분석·Finding을 404로 차단하고, 부여·해제 직후 상태 변화를 전체 pytest로 확인 | 완료 |
| SEC-006 | 비인가 정보 노출 방지 | E2-08, E7-02 | 비인가와 없는 자원의 동일 404 응답 | `backend/tests/test_project_resource_access.py` | 2026-08-27: 없는 프로젝트·분석·Finding 및 타 프로젝트 ID 요청이 동일한 404 정책을 따름을 전체 pytest로 확인 | 완료 |
| SEC-007 | 분석 작업 영역 격리 | E3-02, E3-03, E3-08, E3-05, E4-01, E4-05, E7-03 | 서버 관리 staging·현재 소스 작업영역, 분석별 스냅샷 | `backend/tests/test_source_workspace.py`, `backend/tests/test_source_upload_api.py`, `backend/tests/test_analysis_execution.py` | 2026-08-28: 무작위 staging, 업로드별 불투명 소스 경로, DB commit 실패 정리, 오래된 비참조 경로 정리를 확인. E4-01/05 완료(2026-08-28): 분석별 스냅샷 복사 구현, backend와 Semgrep이 전용 non-root 사용자(`secscan`)로 실행되고(`backend/Dockerfile`), 런타임 저장소가 `source_storage` named volume(`/var/lib/secscan/storage`)으로 코드 bind mount와 분리됨을 `docker-compose.yml`에서 직접 확인(2026-08-28 재현). E3+E4 범위 완료 | 완료 |
| SEC-008 | 파일 경로 검증 | E3-04, E3-05, E3-06, E3-08, E7-03 | 경로 조작, 심볼릭 링크 차단 | `backend/tests/test_source_archive.py`, `backend/tests/test_source_upload_api.py` | 2026-08-28: ZIP Slip, 절대·Windows 경로, NUL, 중복 경로, 링크를 압축 해제 전 거부하고 안전한 오류 응답을 확인 | 완료 |
| SEC-009 | 분석 실행 보호 | E1-04, E2-09, E4-05, E4-06, E4-09, E6-05, E7-04 | 역할별 오류·원본 결과 제한, 자원·시간 제한 | `backend/tests/test_api_contract.py`, `backend/tests/test_project_resource_access.py`, `backend/tests/test_semgrep_runner.py` | 2026-08-27: E2 범위에서 USER의 `error_code`·`error_message`·실행 로그·`raw_result`·`source_snapshot_location` 미노출을 확인. E4-05/06 완료(2026-08-28): `semgrep_wrapper.py`가 `RLIMIT_AS`/`RLIMIT_CPU`를 적용한 뒤 `os.execvp`로 Semgrep을 실행(스레드 앱에서 위험한 `preexec_fn` 미사용), 120초 초과 시 프로세스 그룹 전체를 종료하고 컨테이너 `mem_limit: 1.5g`를 2차 방어로 적용함을 `test_runner_timeout_terminates_the_process_group`, `test_resource_limit_exit_mapping`, `test_execution_log_scrubs_paths_environment_and_archive_name`으로 확인. E2+E4 범위 완료 | 완료 |
| SEC-010 | 외부 구성요소 보안 관리 | E4-02, E7-07 | 버전, 라이선스, 취약점, 업데이트 관리 | `docs/troubleshooting/e4-semgrep-pyjwt-dependency-conflict.md` | E4-02 진행(2026-08-28): Semgrep OSS CLI 1.95.0 버전·라이선스(LGPL 2.1)를 `backend/THIRD_PARTY_NOTICES.md`에 기록. 배포 과정에서 Semgrep-PyJWT 전이 의존성 충돌(및 `pkg_resources` 누락)을 발견해 트러블슈팅 문서로 기록하고 버전 고정(Semgrep 1.95.0, setuptools 80.9.0)으로 해결. 자체 작성 규칙(`RULES_PROVENANCE.md`)은 제3자 라이선스 대상이 아니므로 별도 관리하며, 공식 Semgrep 규칙 팩은 라이선스상 벤더링·재배포가 금지됨을 확인해 채택하지 않음(ADR-023). 전체 의존성 취약점 점검(pip-audit/npm audit 실패 처리)은 E7-07 잔여 | 진행 |

## 테스트 요구사항

| 요구사항 | 이름 | 에픽과 작업 | 구현 대상 | 테스트 | 증거 | 상태 |
|---|---|---|---|---|---|---|
| TST-001 | 인증 기능 시험 | E2-11, E7-01, E7-08 | ADMIN/USER 로그인, 쿠키 세션, 인증 실패와 프론트 보호 경로 | `backend/tests/test_auth_api.py`, `frontend/src/App.test.tsx`, `frontend/src/pages/LoginPage.test.tsx` | 2026-08-27: 백엔드 전체 pytest와 Vitest 15개에서 로그인, 24시간 만료, 변조·누락 쿠키, `/auth/me`, 로그아웃, 401 화면을 확인 | 완료 |
| TST-002 | 역할 권한 시험 | E2-11, E7-01, E7-02 | ADMIN 허용·USER 제한 및 역할별 응답 | `backend/tests/test_auth_authorization.py`, `backend/tests/test_api_contract.py`, `frontend/src/App.test.tsx` | 2026-08-27: ADMIN 2xx, USER 403, DB 역할 기준, USER 민감 필드·관리자 행동 비노출을 확인 | 완료 |
| TST-003 | 프로젝트 접근 시험 | E2-11, E7-02 | 접근권한 부여·해제와 프로젝트 하위 자원 IDOR 차단 | `backend/tests/test_project_access_api.py`, `backend/tests/test_project_resource_access.py` | 2026-08-27: 부여 전 404, 부여 후 200, 해제 후 404와 목록 즉시 제외, 없는 자원의 404를 확인 | 완료 |
| TST-004 | 분석 처리 시험 | E4-09, E5-09, E7-04, E7-06 | 수집, 언어 식별, 분석, 표준화 | `backend/tests/test_analysis_execution.py`, `backend/tests/test_analysis_startup_recovery.py`, `backend/tests/test_semgrep_runner.py` | E4-09 진행(2026-08-28): 분석 실행기 상태 전환, 서버 재시작 복구, Semgrep 명령 구성·대상 파일 필터·타임아웃·자원 제한을 11개 테스트로 검증(2026-08-28 재현: 195개 전체 pytest 통과). 언어별 취약·정상 샘플과 KISA 매핑 기반 표준화 검증(E5-09)은 설계만 확정(ADR-036)되고 구현 잔여 | 진행 |
| TST-005 | 진단 항목 시험 | E5-09, E7-06 | 취약 코드, 정상 코드, 위치와 메타데이터 | 진단 규칙 테스트 | E5 설계 완료(2026-08-28): Java/JavaScript/Python 각 1쌍(외부 입력 taint 취약 예제 vs 고정값 정상 예제)과 KISA 매핑됨/HIGH/UNKNOWN/1건 기대값, 미매핑 보존은 합성 fixture로 검증하는 기준을 확정(ADR-035/036). 실제 fixture 작성과 실행은 E5-09 잔여 | 설계 완료 |
| TST-006 | 개발보안 가이드 진단 기준 카탈로그 시험 | E1-06, E6-07, E7-05 | 49개 식별자, 분류, 번호, 명칭, 상태 | 카탈로그 테스트 | E1-06 진행(2026-08-27): `test_seed_data_has_exactly_49_items`, `test_seed_inserts_exactly_49_catalog_rows`로 49개 항목·고유 `kisa_code`를 검증. 화면 기반 전수 검증은 E6-07/E7-05 잔여 | 진행 |
| TST-007 | 분석 결과 관리 시험 | E4-09, E6-02, E6-08, E6-09, E7-09 | 상태, 저장, 필터, 상세, 기준 연결, ADMIN의 매핑 및 미매핑 원본 결과 조회, USER 응답의 원본 결과 미노출 | 결과 통합 및 역할 응답 테스트 | 화면과 API 결과 | 미착수 |
| TST-008 | 오류 처리 시험 | E4-06, E4-09, E7-04 | 잘못된 소스와 실행 오류 관리 | `backend/tests/test_semgrep_runner.py`, `backend/tests/test_analysis_execution.py` | E4-06/09 진행(2026-08-28): `ANALYSIS_TIMEOUT`(프로세스 그룹 종료), `ANALYSIS_RESOURCE_LIMIT`(RLIMIT 초과), 안전한 오류 메시지 스크러빙(서버 경로·환경변수·업로드 파일명 미노출)을 `test_execution_log_scrubs_paths_environment_and_archive_name`, `test_resource_limit_exit_mapping`으로 확인. `SOURCE_SNAPSHOT_FAILED`/`ENGINE_OUTPUT_INVALID`/`ANALYSIS_INTERRUPTED` 등 나머지 오류 코드의 전수 검증(E5/E7)은 잔여 | 진행 |

## 품질 요구사항

| 요구사항 | 이름 | 에픽과 작업 | 구현 대상 | 테스트 | 증거 | 상태 |
|---|---|---|---|---|---|---|
| QLT-001 | 모듈화 | E1-07, E1-09, E4-03, E5-02 | 인증, 프로젝트, 분석, 결과 모듈 분리 | 모듈 경계 검토 | E1-09 완료(2026-08-27): `app/models`, `app/schemas`, `app/routers`가 도메인별(auth/project/analysis/finding/catalog)로 이미 분리돼 있고, E1-07의 역할별 응답 모델 분리(`AnalysisUserOut`/`AnalysisAdminOut` 등)도 같은 파일 경계 안에서 이뤄졌음을 확인. E4-03 완료(2026-08-28): 분석 실행 로직이 `analysis_executor.py`(오케스트레이션), `semgrep_runner.py`(명령 구성·자원 제한), `semgrep_wrapper.py`(rlimit 적용)로 역할별 분리됨을 확인. 결과 정규화 모듈 분리(E5-02)는 잔여 | 진행 |
| QLT-002 | 진단 항목 독립성 | E1-06, E5-03, E7-06 | 진단 항목별 독립 추가와 테스트 | 항목별 테스트 | E1-06 완료(2026-08-27): `POST /api/catalog`가 항목 하나를 다른 49개 시드 항목과 독립적으로 등록·검증(중복 `kisa_code`만 거부)함을 `test_admin_can_register_new_catalog_item`으로 확인. E5 설계 완료(2026-08-28): `KISA_RULE_MAPPING`이 `UNIQUE(engine, engine_rule_id)`로 규칙 하나가 여러 KISA 항목에 중복 연결되지 않도록 보장하면서도 여러 규칙 → 한 KISA 항목 연결은 허용(ADR-030). 탐지 규칙 단위 독립성 구현은 E5-03/E7-06 잔여 | 진행 |
| QLT-003 | 확장성 | E4-02, E5-02 | 추가 언어와 기준 확장 구조 | `backend/tests/test_semgrep_runner.py` | E4-02 완료(2026-08-28): 언어별 분석 설정(Java/JS/Python 파일 필터)과 자체 규칙 확장 구조(`backend/semgrep-rules/`, 여러 `--config` 파일 지정으로 규칙 추가 가능, 별도 PR·라이선스 검토로 관리)를 실제로 구현. 새 엔진 추가를 위한 파서 확장(E5-02 `NormalizedFinding`)은 설계만 확정 | 진행 |
| QLT-004 | 결과 일관성 | E1-05, E5-02, E5-07 | 공통 위치, 심각도, 신뢰도, 근거 표현 | 결과 일관성 테스트 | E1-05 진행(2026-08-27): Finding에 언어·소스 무관 공통 필드(위치, 심각도, 신뢰도, `evidence`)가 모델 레벨로 확정됨. 언어별 실제 결과 비교는 E5-02/E5-07 잔여 | 진행 |
| QLT-005 | 데이터 정합성 | E1-08, E1-09, E5-06 | 프로젝트, 분석, 결과, 기준 관계와 언어 일관성 | 정합성 테스트 | E1-09 완료(2026-08-27): `test_e1_contract.py` 엔드투엔드 테스트로 저장(모델 생성)과 조회(API 응답)의 필드가 일치함을 프로젝트·접근권한·분석·카탈로그 전체 흐름에서 확인. 분석 시점 언어 일관성(`analyzed_languages`)은 E1-04에서 이미 스냅샷으로 확정. Finding 쪽 언어 일관성 정합성은 E5-06에서 구현·검증됐으며, 2026-08-29 E5 마감 증거를 따른다. | 완료 |

## E5 구현 마감 증거 (2026-08-29)

E5의 백엔드 결과 정규화·KISA 매핑·저장 범위는 완료했다. 아래 요구사항 행은 E6 화면과 E7 통합 검증이 남아 있으면 상태를 `진행`으로 유지하되, E5에 배정된 구현·테스트 근거는 다음과 같다.

| 관련 요구사항 | E5 완료 근거 | 검증 |
|---|---|---|
| SFR-009, SFR-010, SFR-011, SFR-014 | Semgrep JSON을 `NormalizedFinding`으로 파싱하고 KISA 매핑·미매핑 보존·심각도/신뢰도 정규화·코드 조각·안전한 원본 결과를 Finding으로 저장했다. Java, JavaScript, Python 자체 taint 규칙의 고정 `check_id`와 초기 KISA-005/002/043 매핑을 확인했다. | `backend/tests/test_e5_result_normalization.py`, `backend/tests/test_semgrep_runner.py`, `backend/tests/test_analysis_execution.py` 집중 20개 통과 |
| DAR-006, DAR-008, DAR-009 | `engine_rule_id`, `end_line`, 지문, KISA 매핑 테이블과 스냅샷 필드를 Alembic 0008~0011로 적용했다. 결과 정규화·저장은 하나의 트랜잭션으로 처리하고, 잘못된 엔진 출력은 `ENGINE_OUTPUT_INVALID`로 전체 롤백한다. | 전용 PostgreSQL에서 `upgrade head → downgrade 0007 → upgrade head` 통과, `backend/tests/test_migrations.py` 23개 통과 |
| TST-004, TST-005 | 언어별 취약 fixture는 매핑된 HIGH/UNKNOWN Finding 1건을 만들고, 같은 sink를 고정값으로 호출하는 정상 fixture는 해당 규칙이 탐지되지 않음을 확인했다. 합성 JSON fixture로 미매핑 보존과 지문 중복 제거도 확인했다. | E5 집중 20개 통과, 전체 backend pytest 208개 통과 |
| QLT-001, QLT-002, QLT-003, QLT-004, QLT-005 | Semgrep 전용 파서와 엔진 중립 정규화·저장 서비스를 분리하고, `KISA_RULE_MAPPING(engine, engine_rule_id)` UNIQUE와 Finding 지문 UNIQUE로 확장·정합성 경계를 강제했다. | `ruff check app tests`, `git diff --check` 통과 |

## 등록 규칙

- 새 구현을 시작할 때 관련 요구사항 행과 세부 작업의 상태를 함께 갱신한다.
- 테스트를 추가하면 테스트 파일 경로를 기록한다.
- 화면, API 응답, 로그, 캡처는 증거 열에 기록한다.
- 요구사항 해석이 바뀌면 `docs/requirements-interpretation.md`와 관련 ADR을 함께 갱신한다.
- 요구사항을 MVP에서 후순위로 미루더라도 원본 행을 삭제하거나 내용을 축소하지 않는다.
